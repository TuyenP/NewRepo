@model IEnumerable<BigSchool.Tests.Models.Course>
@{
    ViewBag.Title = "Home Page";
}

<div class="row">
    <ul style="margin-top:50px">
        @foreach (var item in Model)
        {
            <li>
                @item.DateTime - @item.Lecturer.Name 
            </li>

        }
    </ul>
</div>